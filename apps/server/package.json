{
   "name": "server",
   "version": "0.0.0",
   "description": "",
   "scripts": {
      "build": "tsup",
      "dev": "npm run build && npm-run-all --parallel watch:build watch:nodemon",
      "remove": "serverless remove",
      "deploy": "serverless deploy",
      "watch:build": "tsup --watch",
      "watch:nodemon": "nodemon dist/devServer.js",
      "clean:unix": "rm -rf node_modules && rm -rf dist",
      "clean:win": "if exist node_modules rmdir /s /q node_modules && if exist dist rmdir /s /q dist"
   },
   "keywords": [],
   "license": "ISC",
   "dependencies": {
      "body-parser": "^1.20.2",
      "cors": "^2.8.5",
      "express": "^4.18.2",
      "serverless-http": "^3.1.1",
      "utils": "*",
      "types": "*"
   },
   "devDependencies": {
      "@types/node": "^20.11.19",
      "npm-run-all": "^4.1.5",
      "tsup": "^8.0.2",
      "@types/express": "^4.17.15",
      "dotenv": "^16.0.3",
      "nodemon": "^2.0.20",
      "serverless-plugin-monorepo": "^0.11.0",
      "serverless-plugin-typescript": "^2.1.4"
   }
}
